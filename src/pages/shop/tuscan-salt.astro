---
const BASE_URL = import.meta.env.BASE_URL;
const PRODUCT = {
  name: "Tuscan Sea Salt",
  price: "$12",
  origin: "Tuscany, Italy",
  image: "images/products/tuscan-salt.jpg",
  stripeLink: "#", // we’ll replace this with your Stripe Payment Link later
  storyUrl: "stories/tuscany",
};

---

<html lang="en">
  <head>
    <title>{PRODUCT.name}</title>
    <meta name="description" content="Tuscan sea salt we discovered during our Tuscany trip." />
  </head>
  <body style="font-family: system-ui; max-width: 960px; margin: 40px auto; padding: 0 16px;">
    <header style="display:flex; justify-content:space-between; align-items:center;">
      <h1>{PRODUCT.name}</h1>
      <nav style="display:flex; gap:12px;">
        <a href={BASE_URL}>Home</a>
        <a href={`${BASE_URL}blog`}>Stories</a>
        <a href={`${BASE_URL}shop`}>Shop</a>
      </nav>
    </header>

    <section style="display:grid; grid-template-columns: 1.2fr 1fr; gap:24px; margin-top:18px;">
      <img src={`${BASE_URL}${PRODUCT.image}`} alt={PRODUCT.name} style="width:100%; border-radius:16px; aspect-ratio: 4/3; object-fit: cover;" />

      <div style="border:1px solid #eee; border-radius:16px; padding:16px;">
        <div style="color:#444;">{PRODUCT.origin}</div>
        <div style="font-size: 22px; margin-top:10px;">{PRODUCT.price}</div>

        <p style="color:#444; line-height:1.5;">
          A clean, mineral-forward salt we picked up in Tuscany. Great on tomatoes, eggs, and grilled veggies.
        </p>

        <a
          href={PRODUCT.stripeLink}
          target="_blank"
          rel="noopener"
          style="display:inline-block; padding:10px 14px; border-radius:12px; background:black; color:white; text-decoration:none; margin-top:10px;"
        >
          Buy with Card (Stripe Checkout)
        </a>

        <div style="margin-top:12px;">
          <a href={`${BASE_URL}${PRODUCT.storyUrl}`}>Read the Tuscany story →</a>
        </div>

        <div style="margin-top:16px; color:#666; font-size:14px;">
          Small inventory. When sold out, checkout will automatically stop (Stripe inventory).
        </div>
      </div>
    </section>
  </body>
</html>

